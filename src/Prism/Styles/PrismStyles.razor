@namespace Skclusive.Script.Prism
@inherits ThemeComponentBase

@if (IsAuto || IsLight)
{
    <PrismLightStyle
        Media="@IsAuto"/>
}

@if (IsAuto || IsDark)
{
    <PrismDarkStyle
        Media="@IsAuto"/>
}
